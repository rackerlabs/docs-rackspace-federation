
[build.environment]
  NPM_VERSION = "6.14.8"
  NODE_VERSION = "12.16.3"

[build]
  publish = "doc/_build/dirhtml"
  command = "cd doc && make clean dirhtml && cd .."

[context.production]
  publish = "doc/_build/dirhtml"
  command = "cd doc && make clean dirhtml && cd .."

[context.deploy-preview]
  publish = "doc/_build/dirhtml"
  command = "cd doc && make clean dirhtml && cd .."
