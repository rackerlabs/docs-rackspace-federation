.. _navigating-gs-ug:

==========
Navigating
==========

Not sure we need this, because the process is pretty straightforward with no
alternate branches to choose from (or have to back out from).  Right?  If so,
I'll delete this page.
